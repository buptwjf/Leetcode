# 二叉树遍历的迭代写法总结
1. 中序遍历递归写法 
   1. 每个元素入栈时候，一直入到他的所有左孩子，直到左孩子为空时：
      ->这时候弹出栈顶元素，然后并访问 -> 每个元素出栈的时候，他的右孩子入栈
2. 前序遍历使用栈
   1. 根节点入栈，当栈不为空时：访问栈顶节点 
      -> 右孩子入栈
      -> 左孩子入栈
3. 后序遍历根据前序遍历改
   1. 前序遍历的 左右函数入栈顺序改变，同时将最终结果翻转