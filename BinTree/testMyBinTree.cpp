#include "BinTree.h"
#include "TreeNode.h"
#include <iostream>
#include <queue>
#include <vector>
using namespace std;
int main(int argc, char const *argv[]) {
    queue<int> q;
    vector<int> a = {1, 0, NULL, 2, NULL};
    for (auto i : a) {
        q.push(i);
    }

    BinTree b ;
    return 0;
}
